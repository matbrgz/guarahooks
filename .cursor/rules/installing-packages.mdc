---
description: 
globs: 
alwaysApply: true
---
To install packages into this project, follow the instructions below:

- Always use `pnpm`;
- If `pnpm` it's not enabled, tell the user.

Example:

```sh
pnpm add ...
```
