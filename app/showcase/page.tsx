import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Showcase',
  description: 'See the apps and websites that use h3/use.',
};

export default function ShowcasePage() {
  return <div></div>;
}
